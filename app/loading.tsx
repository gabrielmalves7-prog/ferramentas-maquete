export default function Loading() {
  return (
    <div
      className="card"
      style={{
        padding: 40,
        textAlign: "center",
        fontFamily: "var(--font-mono)",
        color: "var(--text-3)",
        fontSize: 13,
      }}
    >
      A carregar…
    </div>
  );
}
